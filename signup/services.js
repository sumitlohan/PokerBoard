'use strict';
(function () {
    angular.module("pokerPlanner").service('signupService', [
        'Restangular', 
        
        function(
            Restangular
        ) {
            this.createUser = function(data) {
                // service logic
            };
    }]); 
})();
