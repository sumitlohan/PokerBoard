'use strict';
(function () {
    angular.module("pokerPlanner").service('loginService', [
        'Restangular', 
        
        function(
            Restangular
        ) {
            // this.createUser = function(data) {
            //     // service logic
            // };
    }]); 
})();
